Dataset used from sklearn datasets lfw, which gives the labeled dataset along with the target_names.

The program when ran first downloads the dataset from sklearn.datasets.
After that it will print the average accuracy of the mlp classifier by increasing the n_components from 100 to 150. 